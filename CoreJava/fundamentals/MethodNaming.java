package CoreJava.fundamentals;
/*
 * Naming rules of methods
 * 1) method name should always start with small letters.
 * 2) method name can never start the name with a number. But you can have
 *    numbers after the first letter.
 * 3) only $ and _ are the special characters allowed in name.
 * 4) spaces are not allowed in name.
 * 5) name can never be all number.
 */
public class MethodNaming {

    public void display()
    {

    }
    public static void main(String[] args) 
    {
        
    }
    
}
